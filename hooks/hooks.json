{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "test -f \"$CLAUDE_PLUGIN_DATA/auth.json\" || echo 'archetype: not connected. Run /archetype:setup to get started.'"
          }
        ]
      }
    ]
  }
}
