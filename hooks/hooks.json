{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup",
        "hooks": [
          {
            "type": "command",
            "command": "test -n \"$ARCHETYPE_PORTAL_URL\" && test -n \"$ARCHETYPE_API_KEY\" || echo 'archetype-feature-validation: ARCHETYPE_PORTAL_URL and ARCHETYPE_API_KEY env vars are not set — portal calls will fail until they are configured.'"
          }
        ]
      }
    ]
  }
}
